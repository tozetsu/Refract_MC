{
  "name": "@refract/electron-bridge",
  "version": "1.3.1",
  "description": "Final Electron bridge that installs the Tauri build.",
  "author": "ShevRuslan1",
  "homepage": "https://github.com/RefractMC/Refract_MC",
  "private": true,
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win nsis --publish never -c.win.signAndEditExecutable=false",
    "build:signed": "electron-builder --win nsis --publish never",
    "pack": "electron-builder --dir"
  },
  "devDependencies": {
    "electron": "42.4.1",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "com.refract",
    "productName": "Refract",
    "copyright": "Copyright (c) Refract",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "asar": true,
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "../tauri/src-tauri/icons/icon.ico"
    },
    "nsis": {
      "artifactName": "Refract-Setup-${version}.${ext}",
      "oneClick": true,
      "perMachine": false,
      "allowElevation": true,
      "deleteAppDataOnUninstall": false
    },
    "publish": [
      {
        "provider": "github",
        "owner": "RefractMC",
        "repo": "Refract_MC"
      }
    ]
  }
}
